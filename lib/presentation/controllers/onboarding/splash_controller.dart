

import 'package:riverpod_clean_architecture_lib/base/base_controller.dart';

import '../../states/onboarding/splash_state.dart';

class SplashController extends BaseController<SplashState> {
  SplashController({required SplashState state}): super(state);

  @override
  onInit() {

    return super.onInit();
  }

  @override
  void dispose() {

    super.dispose();
  }
}