name: template
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_riverpod: ^2.1.3
  firebase_crashlytics: ^3.0.2
  firebase_remote_config: ^3.0.2
  firebase_core: ^2.1.0
  firebase_analytics: ^10.0.2
  riverpod: ^2.1.3
  logger: ^1.1.0

  daniel_ui_template_lib:
    git:
      url: https://github.com/donghyun-Kim0405/daniel_ui_template_lib.git
      ref: master

  riverpod_clean_architecture_lib:
    git:
      url: https://github.com/donghyun-Kim0405/riverpod_clean_architecture_lib.git
      ref: master


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:


  uses-material-design: true

